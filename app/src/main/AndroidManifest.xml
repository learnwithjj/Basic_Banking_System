<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.basicbankingsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_pay_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_pay_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasicBankingSystem">
        <activity
            android:name=".allDetails"
            android:exported="true" />
        <activity
            android:name=".transfer"
            android:exported="true" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>